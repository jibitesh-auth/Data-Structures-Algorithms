#include<bits/stdc++.h>

using namespace std;

int main() {
    // int bdate, num;
    // double bdate, num;
    // long bdate, num;
    // char bdate, num;
    // string bdate, num;
    // cin >> bdate >> num;
    // cout << bdate << endl << num;

    //entire string
    string str, str2;
    getline(cin,str);
    getline(cin,str2);
    cout << str << endl << str2;
    return 0;
}